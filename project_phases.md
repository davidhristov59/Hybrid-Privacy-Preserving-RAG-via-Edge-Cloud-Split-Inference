TODO 

--> env file
VAULT_PATH = "edge/vault/identity_vault.json"
# OPENAI_API_KEY = "sk-..." (Use .env file instead)

--> fix the FE 
	- React (Next.js / Vite + UI Library)
	   * Pros: Total control over UI/UX, easier to scale to thousands of users.
	   * Cons: Requires setting up a Node.js environment, writing API hooks, handling CORS, and significantly more code (boilerplate).

	 OR CHANGE THE STREAMLIT (REFACTOR IT)


--> add evaluation metrics for the project 

--> structure the scripts in folders and remove boilerplate code 
	--> remove csv and pdf folders in processes data if needed (i don't know why are they there )

--> Naucniot trud da se napise (JAS)

--> commands for starting backend and FE 
	--> Dockerize it so it can start with one command 